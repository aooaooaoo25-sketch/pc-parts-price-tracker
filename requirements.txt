# PC 零件二手市場價格追蹤系統 — Python 依賴
# 安裝： pip install -r requirements.txt

# --- 目前實際使用 ---
aiohttp>=3.9            # 非同步 HTTP 爬蟲
beautifulsoup4>=4.12    # HTML 解析（bs4，目前使用內建 html.parser）

# --- 未來功能預留（程式尚未啟用，需要時取消註解）---
# lxml>=5.0             # 更快的 HTML 解析器；啟用後可將 BeautifulSoup(..., "lxml")
# python-dotenv>=1.0    # 從 .env 載入 ANTHROPIC_API_KEY 等環境變數
